SELECT DEPARTMENT,
       EMPLOYEE_COUNT
FROM (
    SELECT DEPARTMENT,
           COUNT(*) AS EMPLOYEE_COUNT
    FROM EMPLOYEES
    GROUP BY DEPARTMENT
) AS T
WHERE EMPLOYEE_COUNT > (
    SELECT AVG(EMPLOYEE_COUNT)
    FROM (
        SELECT COUNT(*) AS EMPLOYEE_COUNT
        FROM EMPLOYEES
        GROUP BY DEPARTMENT
    ) AS X
);