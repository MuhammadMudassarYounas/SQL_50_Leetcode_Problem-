SELECT DEPARTMENT,
       SUM(SALARY) / COUNT(DEPARTMENT) AS AVERAGE_SALARY
FROM EMPLOYEES
GROUP BY DEPARTMENT;




SELECT DEPARTMENT,
       AVG(SALARY) AS AVERAGE_SALARY
FROM EMPLOYEES
GROUP BY DEPARTMENT;